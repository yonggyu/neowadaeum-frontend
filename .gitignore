# ── 의존성 · 빌드 산출물 ─────────────────────────────────────
node_modules/
dist/
dist-ssr/
*.local
coverage/

# ── 시크릿 (§7.1 — 실제 값은 .env 에만, 커밋하지 않는다) ─────
.env
.env.*
!.env.example

# ── 생성물 ───────────────────────────────────────────────────
# 계약에서 만들어지는 타입. 소스가 아니라 산출물이며, 계약이 진실의 원천이다.
src/api/schema.d.ts

# tsc -b 의 증분 빌드 캐시. 머신마다 다르고 커밋할 이유가 없다.
*.tsbuildinfo

# ── 에디터 · OS ──────────────────────────────────────────────
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log
