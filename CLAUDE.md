# CLAUDE.md — 너와다음 Frontend (v0.2)

사용자가 선택지로 이야기를 이끄는 **AI 인터랙티브 스토리 플랫폼**의 프론트엔드다.
한국 서비스, 15세 이용가 단일 등급. **Vite + React + TypeScript (SPA).**

**백엔드는 별도 레포다** — `neowadaeum-backend`. 나란히 클론했다고 가정한다
(`../neowadaeum-backend`).

> **이 파일만 항상 로드된다.** 나머지는 필요할 때 경로로 찾아 읽는다. `@` import 를 쓰지 않는 이유가 이것이다.

## Source of Truth

```
CLAUDE.md(이 파일) > 백엔드의 docs/corrections.md > 백엔드의 docs/openapi.yaml
```

**정정본이 계약을 정정한다. 충돌하면 `corrections.md` 가 이긴다** — 백엔드의 순서와 같다.
두 레포가 같은 사실에 다른 답을 내지 않게 하려는 것이다.

그 아래에서 **계약이 화면을 이긴다.** API 타입을 손으로 적지 않고 `npm run api:types` 로 만든다.
계약과 화면이 어긋나면 프론트에서 우회하지 않는다 — 우회하면 두 곳에 서로 다른 진실이
생기고, 그때부터 어느 쪽이 맞는지 매번 문제가 된다. 계약이 틀렸다고 판단되면 **백엔드
레포에 이슈를 연다.**

## 불변 규칙 F-1 ~ F-8

**이 규칙을 깨는 PR 은 반려된다.**

| # | 규칙 |
|---|---|
| **F-1** | 선택지는 서버가 준 `choiceId` 로만 제출한다. 화면에 보이는 `text` 를 보내지 않는다 (백엔드 I-1) |
| **F-2** | API 타입을 손으로 적지 않는다. 계약에서 생성한다 |
| **F-3** | 토큰을 `localStorage` · `sessionStorage` 에 두지 않는다. 메모리에만 둔다 |
| **F-4** | 오류 화면은 서버의 `error` 코드로 분기하고 `message` 를 그대로 보여 준다. 문구를 프론트가 지어내지 않는다 |
| **F-5** | 세이프티 차단 사유를 구체적으로 표시하지 않는다. 서버가 준 것 이상을 추측해 보여 주지 않는다 (백엔드 R9.6) |
| **F-6** | `player_ref` 를 화면·URL·로그에 노출하지 않는다. 작성자는 `authorDisplayName` 뿐이다 |
| **F-7** | 재시도가 있는 요청(턴 생성)에는 `Idempotency-Key` 를 붙인다. 붙이지 않으면 중복 과금이 된다 |
| **F-8** | 개발 서버 프록시로 CORS 를 우회하지 않는다. 백엔드가 정식으로 열었고, 프록시는 dev 에서만 통한다 |
| **F-9** | 화면은 **390 · 768 · 1024 · 1440** 네 폭에서 모두 성립한다. 한 폭만 맞춰 만들지 않는다. 폭별 규칙은 와이어프레임 `1k` · `2f` 가 정한다 |

**예외가 필요하면 코드를 쓰지 말고 먼저 보고한다.** 예외를 받아들이기로 했다면 그 이슈에
ADR 형식(맥락 · 결정 · 대안 · 결과)으로 근거를 남긴다 — 무엇을 포기했는지가 남지 않은
예외는 다음 사람에게 그냥 규칙이 없는 것으로 보인다.

## 작업 시작 전

1. **이슈 번호를 확인했는가.** 이슈 없이 코드를 쓰지 않는다. `git status` · 현재 브랜치를 먼저 본다
2. **계약을 먼저 읽었는가.** `../neowadaeum-backend/docs/openapi.yaml` 의 해당 오퍼레이션,
   그리고 그것을 정정하는 `docs/corrections.md` 항목
3. **불변 규칙을 위반하는가.** 불가피하면 코드를 쓰지 말고 먼저 보고한다
4. **디자인이 있는가.** 없으면 화면을 지어내지 않는다 — 골격까지만 만들고 보고한다
5. **작업 중 다른 문제를 발견하면 그 자리에서 고치지 않는다.** 새 이슈 후보로만 보고한다

## 반응형 — 어느 폭에서도 지키는 것

모바일과 PC 를 **같은 화면의 두 폭으로** 만든다. 한쪽을 먼저 만들고 다른 쪽을 나중에
맞추지 않는다 — 그렇게 하면 나중 쪽이 언제나 타협본이 된다.

- **Story 영역에 fixed height 를 두지 않는다.** `min-height` 만 쓴다
- 본문 읽기 폭은 `--read-width`(62ch), 중앙 정렬
- Visual 은 **비율로만** 고정한다 (16:9 · 4:5). 높이를 px 로 박지 않는다
- Choice 는 항상 본문 끝에 둔다. sticky 로 띄우지 않는다 — 읽기를 방해한다
- Mobile 은 Choice 개수와 무관하게 **세로 full-width**. 터치 타깃 최소 높이 60px
- 본문을 탭해도 진행되지 않는다. **진행은 Choice 로만** 한다

폭별 차이는 `1k`(Library 열 수 · 본문 padding)와 `2f`(Play)를 따른다. 값을 화면이 직접
적지 말고 `src/styles/tokens.css` 의 토큰을 쓴다.

## 백엔드에 대해 알아야 하는 것

| | |
|---|---|
| 인증 | Bearer 토큰. **쿠키를 쓰지 않는다.** 리프레시 저장소도 없다 |
| 로그인 | Google OAuth 하나. **dev 우회가 없다** — 실제 OAuth 앱이 필요하다 |
| 오류 형태 | `{ error, message, details }` 하나. **셋 다 필수**이고 `details` 는 `null` 이 되지 않는다(없으면 `{}`). 코드 필드 이름은 `code` 가 아니라 **`error`** 다. 스택트레이스·SQL·내부 경로가 없다 |
| 추적 | 응답의 `X-Request-Id`. CORS 노출 헤더에 있으므로 스크립트가 읽을 수 있다 |
| CORS | `app.cors.allowed-origins` 에 이 오리진이 있어야 한다. 자격 증명은 허용하지 않는다 |
| 턴 | 사용자 입력면은 `choiceId` 하나뿐이다. 플레이 중 자유입력이 없다 |
| 지연 | 턴 생성은 **모델을 기다린다.** 서버 예산 25초 · 동기 28초. 로딩 상태가 화면의 기본값이다 |

## 설계 원칙 — Clean Code / Pragmatic Clean Architecture

이 프로젝트는 장기 확장을 전제로 한다. 새 화면을 추가할 때 기존 핵심 흐름의 변경 범위를
최소화할 수 있도록 책임과 의존성을 명확히 분리한다.

**단, 미래의 가능성만을 이유로 추상화를 미리 만들지 않는다.** 클린 아키텍처를 명분으로 한
오버엔지니어링은 금지한다.

### 의존성

- 의존성은 바깥쪽(화면)에서 안쪽(계약) 방향으로 흐른다.
- `src/api/**` 는 컴포넌트를 알지 않는다. React 를 import 하지 않는다.
- 계약의 변경이 화면 전체로 번지지 않도록 경계를 둔다 — 생성 타입은 `api/` 와 그것을 부르는
  훅까지 흐르고, 그 아래로는 화면이 실제로 쓰는 모양으로 좁혀 넘긴다.
- 서버 응답 객체를 그대로 여러 컴포넌트에 흘리지 않는다. 명시적인 타입으로 전달한다.

### 책임

- 컴포넌트와 훅은 하나의 명확한 책임을 갖는다.
- **컴포넌트** — 렌더링과 사용자 입력.
- **훅 / 상태** — 유스케이스와 화면 상태 전이.
- **`api/`** — 계약 호출과 오류 변환.
- `fetch` · 라우터 · 스토리지의 세부 구현을 컴포넌트로 누출하지 않는다.

### 확장

변경 가능성이 **실제로 존재하는** 경계에는 인터페이스를 둘 수 있다 — API 클라이언트 ·
토큰 보관 방식 · 인증 Provider · 오류 문구 매핑.

새 엔드포인트를 붙일 때 기존 화면 로직을 고치는 대신 **새 호출을 더하는 방식으로** 확장할
수 있어야 한다.

### 추상화 제한

**다음 이유만으로 훅 · 래퍼 컴포넌트 · Context · 제네릭 유틸을 만들지 않는다.**

- "나중에 필요할 수도 있어서"
- 테스트하기 편할 것 같아서
- Clean Architecture 이므로
- 모든 컴포넌트에 전용 훅이 있어야 할 것 같아서

추상화는 다음 중 하나가 있을 때 도입한다.

1. 실제 사용처가 둘 이상 존재한다.
2. 이슈 요구사항에 명확한 교체 가능성이 정의되어 있다.
3. 외부 시스템(계약 · 브라우저 API)과 화면 사이의 경계를 보호해야 한다.
4. 테스트가 아니라 실제 설계상의 의존성 역전이 필요하다.

### 코드 품질

- 의미가 드러나는 이름을 사용한다.
- boolean prop 으로 한 컴포넌트에 여러 책임을 넣지 않는다.
- 긴 컴포넌트는 책임 단위로 분리한다. 파일을 쪼개는 것이 목적이 아니다.
- **중복 제거보다 잘못된 추상화를 만들지 않는 것을 우선한다.** 공통화는 의미가 동일할 때만
  한다 — 마크업 모양이 비슷하다는 이유로 합치지 않는다.
- 변경과 무관한 리팩터링은 현재 이슈에 포함하지 않는다. 필요하면 **이슈 후보로 보고한다.**

### 우선순위

**설계 원칙은 작업 범위를 확대하는 근거가 될 수 없다.**

```
불변 규칙 · 보안 > 이슈 요구사항과 DoD > 단순하고 명확한 구현 > 재사용과 추상화
```

"더 깨끗한 구조"를 이유로 현재 작업과 관계없는 코드를 수정하지 않는다.

## 보안 hard-stop

- **시크릿을 소스에 커밋하지 않는다.** 실제 값은 `.env` 에만. 푸시된 자격 증명은 **삭제가
  아니라 로테이션**이다
- **`${VAR:기본값}` 패턴 금지.** 값이 없으면 실패시킨다
- **토큰·이메일·`player_ref` 를 콘솔 로그에 남기지 않는다**
- **S-11 — 이 레포는 공개다.** 커밋·이슈·PR·주석·문서가 즉시 세계에 읽힌다. 다음을 **적지
  않는다** — 세이프티 우회 방법, 블록리스트 실제 항목, 정규화를 뚫는 표기, 미수정 취약점의
  재현 절차, 운영 도메인 · 계정 체계
- **백엔드 `docs/internal/` 의 내용을 옮기지 않는다.** 인용이 필요하면 **조항 번호로만
  지목한다** (예: "R9.6 의 차단 응답")

## 개발 루프

```
계약 확인 → 타입 생성 → 최소 구현 → 타입검사·린트·테스트 → 셀프 리뷰 → PR
```

```bash
npm run api:types                          # 계약이 바뀌었으면 먼저
npm run typecheck && npm run lint && npm test
```

- **범위 밖 문제는 고치지 말고 이슈 후보로 보고한다.**
- **미구현을 통과시키지 않는다.** 빈 컴포넌트 · `return null` · 하드코딩한 가짜 응답으로
  넘어가지 말고 명시적으로 실패시킨 뒤 이슈를 만든다. 프론트에서 특히 위험하다 — 비어 있는
  화면은 **돌아가는 것처럼 보인다.**
- 요구사항 ID(`F-1`, 백엔드의 `R9.6`, 계약 오퍼레이션 ID)를 코드 주석 또는 테스트 이름에
  남긴다 — 예: `F1_choiceId_만_전송한다()`.

## Git

이슈 → 브랜치(`<타입>/#<번호>-<슬러그>`) → 커밋(타입검사 통과 시점마다) → PR.
**`frontend` / `dev` / `main` 에 직접 푸시하지 않는다.** 커밋은 Conventional Commits + 이슈 번호.
**PR 400줄 — `src/**` 기준이다.** 테스트·설정·생성물(`src/api/schema.d.ts`)은 세지 않는다.
브랜치 수명 3일.
CI 잡 이름 셋(`build` · `test` · `gitleaks`)은 브랜치 보호의 필수 체크 이름이므로 **바꾸지 않는다.**

## 프로젝트 위키 — **작업 PR 마다 갱신한다**

두 레포 밖에 **너와다음 전용 위키**가 있다. 코드와 계약이 *무엇을* 하는지 말한다면, 위키는
**6개월 뒤에 그 결정을 재구성할 수 있게** 정리해 둔 곳이다. 백엔드와 **같은 위키**이며
프론트도 자기 몫을 갱신한다.

주소·토큰은 레포 밖 로컬 설정에 있다 (S-11). 접근 수단은 PATH 의 `wiki` CLI 하나다.

```bash
wiki search  neowadaeum "<주제>"
wiki read    neowadaeum 50-API/error-codes.md
wiki build   neowadaeum                     # 링크·Mermaid 검증. 갱신했으면 반드시 돌린다
wiki publish neowadaeum "<커밋 메시지>"
```

> **`llmwiki` 가 아니다.** React·프론트엔드 일반론은 이 위키에 넣지 않는다 — 너와다음을
> 그만두면 쓸모없어지는 것만 여기 온다.

**작업 정의를 읽기 전에 위키를 먼저 검색한다.** 이미 정리된 결론이 있으면 일반론을 새로
세우지 말고 그 결정을 따르고, 참조한 노트를 PR 본문에 밝힌다. **충돌하면 레포가 이긴다.**

### 프론트 PR 이 갱신하는 페이지

**Draft → Ready 전환 조건이다.** 갱신할 것이 없으면 **PR 본문에 "없음"이라고 적는다.**
판단 자체를 건너뛰지 않는다.

| 이 PR 이 바꾼 것 | 갱신할 위키 페이지 |
|---|---|
| 작업 완료 · 이슈 개폐 · 남은 결정 | `00-Overview/current-status` — **기준 커밋·날짜를 함께 고친다** |
| 계약 소비 방식 · `ErrorCode` 화면 매핑 | `50-API/*` |
| 새 ADR · 기존 ADR 이 `superseded` 됨 | `30-Decisions/adr-*` + `30-Decisions/index` |
| 새 용어 또는 이름 변경 | `00-Overview/glossary` |
| 계약과 화면의 어긋남을 발견하거나 해소함 | `00-Overview/doc-code-drift` |
| 브랜치 · CI · 테스트 정책 | `70-Development/*` |

### 쓸 때의 규칙

- **백엔드 `docs/internal/` 원문을 옮기지 않는다.** 조항 번호로만 지목한다. 세이프티
  임계값·블록리스트 항목·시크릿도 적지 않는다 — 위키가 토큰 게이트 뒤에 있어도 같다 (S-11).
- **근거를 남긴다.** 파일 또는 `ADR`/`F`/`R` 조항 단위로. **줄 번호는 쓰지 않는다.**
- **canonical 을 하나만 둔다.** 복붙하지 말고 링크한다.
- **시그니처·props 목록·디렉터리 트리를 복사하지 않는다.** 반드시 실제와 어긋나고,
  **어긋난 문서는 없는 것보다 나쁘다.** 대신 *왜 그런가*를 쓴다.
- 새 노트를 만들면 **그 폴더 `index.md` 에 링크를 추가한다.** 파일명은 소문자+하이픈,
  한글 파일명 금지.

**CI 는 이것을 검증하지 못한다** — 위키가 레포 밖이기 때문이다. `wiki build neowadaeum` 이
유일한 게이트이고, 나머지는 PR 체크리스트가 강제한다.

## 도구 사용

**2~3개 파일만 읽으면 되는 일에 서브에이전트를 띄우지 않는다.**
