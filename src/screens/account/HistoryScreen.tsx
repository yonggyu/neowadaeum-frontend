/**
 * History
 *
 * 자리 표시자다. 라우트는 바닥(이슈 #4)에서 미리 다 뚫었고, 이 파일은 C 슬라이스가 채운다.
 * 화면을 지어내지 않는다 — 와이어프레임 2e 를 그대로 옮긴다.
 */
export function HistoryScreen() {
  return <main data-screen="HistoryScreen" />
}
